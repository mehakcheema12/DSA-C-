#include <iostream>
using namespace std;

int main() {
    char c1, c2;
    cout << "Enter two characters: ";
    cin >> c1 >> c2;
    cout << "Difference: " << abs(int(c1) - int(c2));
    return 0;
}
